export const vampReducer = (state = {vamps: [], status: null}, action) => {
  switch(action.type){
    case 'ADD_VAMP':
      return
    case 'SENDING_VAMP':
      return 
    default:
      return state
  }
}